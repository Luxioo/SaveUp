<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SaveUp.ViewModel"
             x:Class="SaveUp.ListePage">

    <ContentPage.BindingContext>
        <local:MainPageViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Editor IsReadOnly="True" x:Name="ed_te" Text="{Binding ListeasString}" />
            <ListView x:Name="ItemListelistView"
                      ItemTapped="ItemListelistView_ItemTapped"
                      ItemsSource="{Binding ItemListe}" />

            <Button Text="Liste Leeren" x:Name="bt_le" Command="{Binding Commandlisteleeren}" />
            <Button Text="Speichern lokal" x:Name="bt_sp" Command="{Binding Commandspeichern}" />
            <Button Text="Zurück zu der Eintragsseite" x:Name="bt_ei" Command="{Binding CommandzurückHauptseite}" />
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>